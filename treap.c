#include <stdio.h>
#include <stdlib.h>

typedef struct treap{
    int info;
    int chave;
    struct treap *direita;
    struct treap *esquerda;
}treap;

int main(){
    treap *arvore = NULL;

    

    return 0;
}